<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>{{ request.host_url }}</loc><changefreq>daily</changefreq><priority>1.0</priority></url>
  <url><loc>{{ url_for('public.search', _external=True) }}</loc><changefreq>daily</changefreq><priority>0.9</priority></url>
  {% for cat in categories %}
  <url><loc>{{ url_for('public.category', slug=cat.slug, _external=True) }}</loc><changefreq>weekly</changefreq><priority>0.8</priority></url>
  {% endfor %}
  {% for m in merchants %}
  <url><loc>{{ url_for('public.merchant_profile', slug=m.slug, _external=True) }}</loc><changefreq>weekly</changefreq><priority>0.7</priority></url>
  {% endfor %}
</urlset>
